#include<stdio.h>
#include<limits.h>
#define V 5

int minkey(int key[],int visited[]){
int min=INT_MAX,min_index,min_index;
for(int i=0;i<V;i++){
if(!visited[i] && key[i]<min){
min=key[i];
min_index=i;
}
}
return min_index;
}

void MST(int graph[V][V]){
int parent[V];
int key[V];
int visited[V];
for (i=0;i<V;i++){
key[i]=INt_MAX;
visited[i]=0;
}
key[0]=0;
parent[0]=-1;
//minimun spanning tre cotains V-1 edges
for(int count=0;count<V-1;count++){
int u=minkey(key,visited);//pick the vertex
visited[u]=1;
for(int v=0;v<V;v++){
if (gragh[u][v] && visited[v] && graph[u][v]<key[v]){
parent[v]=u;
key[v]=graph[u][v];
}
}
}
void printprimsMST(int parent[],int graph[V][V]){
printf("\nprim's MSt :\n");
printf("edge \t weigt:\n);
int total=0;
for (int i=1;i<V;i++)
{
printf("%d%d\t%d\n",parent[i],i,grapht[i][parent[i]]);
total+=graph[i][parent [i]];
}
printf("total cost=%d\n",total);
}

printprimMST(parent, graph);
}
int main(){
int graph[V][V]={
{0,2,3,0,0},
{2,0,5,3,0},
{3,5,0,0,4},
{0,3,0,0,3},
{0,0,4,2,0}}
primMST(graph);
return 0;
}
